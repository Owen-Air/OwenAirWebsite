---
interface Props {
  eyebrow?: string;
  title: string;
  subtitle: string;
  primaryCta?: { href: string; label: string };
  secondaryCta?: { href: string; label: string };
  compact?: boolean;
}

const {
  eyebrow = 'Isle of Man technical specialist',
  title,
  subtitle,
  primaryCta,
  secondaryCta,
  compact = false
} = Astro.props;
---

<section class="hero section">
  <div class:list={['container', compact ? 'hero-compact' : 'hero-grid']}>
    <div class="flow">
      <p class="pill">{eyebrow}</p>
      <h1>{title}</h1>
      <p class="hero-subtitle">{subtitle}</p>
      <div class="hero-ctas">
        {primaryCta && (
          <a class="button button-primary" href={primaryCta.href}>
            {primaryCta.label}
          </a>
        )}
        {secondaryCta && (
          <a class="button button-secondary" href={secondaryCta.href}>
            {secondaryCta.label}
          </a>
        )}
      </div>
    </div>

    {!compact && (
      <aside class="card hero-note" aria-label="Trust highlights">
        <h2>Trusted practical support</h2>
        <ul class="check-list">
          <li>Isle of Man based — local response and accountability</li>
          <li>Hands-on diagnostics with plain-English explanations</li>
          <li>Staged upgrades, not unnecessary replacement</li>
        </ul>
      </aside>
    )}
  </div>
</section>

<style>
  .hero {
    padding-top: clamp(3.5rem, 7vw, 6rem);
  }

  .hero-grid {
    display: grid;
    gap: 1.2rem;
    align-items: end;
    grid-template-columns: 1.25fr minmax(16rem, 0.85fr);
  }

  .hero-compact {
    max-width: 44rem;
  }

  h1 {
    margin: 0;
    max-width: 22ch;
    font-family: 'Sora', sans-serif;
    font-size: clamp(2rem, 4.4vw, 3.5rem);
    line-height: 1.04;
    letter-spacing: -0.03em;
  }

  .hero-subtitle {
    margin: 0;
    max-width: 68ch;
    color: var(--text-muted);
    font-size: clamp(1rem, 1.8vw, 1.16rem);
  }

  .hero-ctas {
    margin-top: 1.15rem;
    display: flex;
    flex-wrap: wrap;
    gap: 0.65rem;
  }

  .hero-note h2 {
    margin: 0 0 0.9rem;
    font-family: 'Sora', sans-serif;
    font-size: 1.04rem;
  }

  @media (max-width: 58rem) {
    .hero-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
